# TODO - 31 janvier 2026

## Statut actuel

**Version:** v0.1.0-alpha
**Phases complétées:** 1-4
**Phase en cours:** 5-6 (Typst package + inline expressions)

### Phases complétées ✅

1. ✅ **Phase 1-4:** Exécution R, caching SHA256, dépendances, graphics
2. ✅ **Parser winnow:** Migration du parser regex → winnow (robuste, position tracking)
3. ✅ **Nouvelle syntaxe inline:** `#r[code]` → `` `{r} code` `` (backticks)
4. ✅ **Backend trait:** Architecture modulaire pour supporter plusieurs formats de sortie
5. ✅ **LSP Phase 1-3:** Proxy tinymist, hover, completion, diagnostics, formatting

### Progrès Phase 6 (Inline expressions) 🚧

**Statut:** 80% complété

✅ **Complété:**
- Parser winnow pour `` `{r} code` ``
- Extraction des expressions inline (UTF-16 aware pour LSP)
- Syntaxe mise à jour dans REFERENCE.md

❌ **À faire:**
- [ ] Parsing des options inline (`` `{r echo=false} code` ``)
- [ ] Tests inline avec options
- [ ] Mise à jour README.md (encore `#r[expr]` à la ligne 249)

## Issues identifiées (par priorité)

### CRITIQUE 🔴

1. **Duplication code lecture R**
   - Fichiers: `execution.rs`, `process.rs`
   - Impact: Maintenance difficile, bugs potentiels
   - Action: Extraire dans une fonction commune

2. **Tests manquants LSP**
   - Modules: `position_mapper`, `handlers`, `transform`
   - Impact: Régression possible
   - Action: Ajouter tests unitaires

### HAUTE ⚠️

3. **Perte silencieuse output texte**
   - Fichier: `output_parser.rs`
   - Problème: Si parsing JSON échoue, output texte perdu
   - Action: Logger ou préserver le texte brut

4. **Tests manquants backend.rs**
   - Impact: Couverture incomplète
   - Action: Ajouter tests unitaires

### MOYENNE 🟡

5. **Gestion erreurs process.rs**
   - Problème: Panics potentiels sur unwrap()
   - Action: Utiliser ? et gérer les erreurs proprement

6. **Validation options chunks**
   - Problème: Options invalides acceptées silencieusement
   - Action: Valider et retourner erreurs explicites

## Structure de projet (Décisions)

### Philosophie adoptée: **Option 1 - Minimal**

> **Knot = compilateur R↔Typst, PAS orchestrateur de documents**

**Principes:**
- Knot ne gère PAS la structure Typst (dossiers, images, bibliographie)
- L'utilisateur utilise le système de projet Typst natif
- Knot fournit juste les helpers (knot.typ, knot.R)

### Template minimal

**Structure:**
```
my-project/
├── knot.toml           # Configuration Knot
├── main.knot           # Document principal
└── lib/
    ├── knot.typ        # Helpers Typst (copie locale)
    └── knot.R          # Helpers R (copie locale)
```

**knot.toml:**
```toml
[document]
main = "main.knot"

[helpers]
typst = "lib/knot.typ"
r = "lib/knot.R"
```

**Pourquoi copie locale ("poorman"):**
- Pas encore publié sur CRAN/Typst Universe
- Permet développement indépendant
- Transition facile vers packages publiés plus tard:
  - `typst = "@preview/knot"` (futur)
  - `r = "@cran/knot"` (futur)

### Distribution template

**Stratégie: A + C (Hybride)**
- **A:** Embarqué dans le binaire (via include_dir!)
- **C:** Override possible dans `~/.knot/templates/`

**Commande:**
```bash
knot init my-project         # Crée le projet avec template minimal
```

## Travaux complétés (session 31/01)

### 1. Documentation syntax update ✅

**Fichiers modifiés:**
- README.md
- REFERENCE.md
- examples/tidymodels_demo/README.md
- crates/knot-core/tests/README.md
- editors/README.md
- editors/vscode/TESTING.md
- editors/vscode/QUICKSTART.md

**Changement:**
- Ancien: `#r[expr]`
- Nouveau: `` `{r} expr` ``
- Note ajoutée: Accolades `{r}` REQUISES (`` `r code` `` géré par Typst)

### 2. Architecture Backend ✅

**Ajout trait Backend:**
```rust
pub trait Backend {
    fn render_chunk(&self, chunk: &Chunk, output: &str) -> String;
    fn render_inline(&self, expr: &str, result: &str) -> String;
}
```

**Implémentation:**
- `TypstBackend` (actif)
- Préparation pour `LaTeXBackend`, `HTMLBackend` (futur)

### 3. Parser winnow ✅

**Migration complète:**
- Suppression parser regex
- Parser combinator winnow (v0.7)
- Position tracking précis (line/column)
- Gestion UTF-16 pour LSP

### 4. LSP synchronisation parfaite ✅

**Smart padding:**
- Remplacement caractères par espaces (préserve UTF-16 count)
- `\n` préservé → numéros de lignes corrects
- `PositionMapper` simplifié (quasi-identité)

### 5. VS Code extension syntax ✅

**Mise à jour:**
- `knot.tmLanguage.json` → support backticks
- Highlighting pour `` `{r} code` ``

## Prochaines étapes (prioritaires)

### Avant Phase 6

1. **Implémenter `knot init`** (haute priorité)
   - Créer structure projet minimal
   - Copier template depuis binaire
   - Copier packages R et Typst depuis sources
   - Validation chemins helpers dans knot.toml

2. **Parsing options inline** (Phase 6)
   - Parser `` `{r key=value, key=value} code` ``
   - Format valeurs: JSON-like (true, false, 0, "string")
   - Options supportées: echo, cache, digits

3. **Mise à jour README.md**
   - Ligne 249: `#r[expr]` → `` `{r} expr` ``
   - Section inline expressions avec exemples

### Tests

4. **Tests LSP** (haute priorité)
   - `position_mapper_tests.rs`
   - `handlers/hover_tests.rs`
   - `handlers/completion_tests.rs`
   - `transform_tests.rs`

5. **Tests backend.rs**
   - Render chunks
   - Render inline
   - Multiple backends

### Refactoring

6. **Déduplication code R**
   - Extraire lecture output R
   - Fonction commune pour `execution.rs` et `process.rs`

7. **Amélioration gestion erreurs**
   - Validation options chunks
   - Erreurs explicites (pas de panics)
   - Logging proper

## Commandes à implémenter

### Priorité 1 (maintenant)

```bash
knot init <project-name>     # Crée projet minimal
knot compile <file.knot>     # Déjà implémenté ✅
```

### Priorité 2 (après Phase 6)

```bash
knot watch <file.knot>       # Compilation auto (Phase 6)
knot clean                   # Déjà implémenté ✅
knot install-vscode-extension  # Installation extension VS Code
```

### Priorité 3 (futur)

```bash
knot preview <file.knot>     # Prévisualisation live (Phase 7?)
knot publish                 # Publier sur Typst Universe (Phase 9?)
```

## Notes techniques

### Syntaxe inline complète

**Évaluation simple:**
```typst
`{r} 1+1`  →  2
```

**Avec options:**
```typst
`{r, echo=true} mean(x)`  →  5.5  // + code affiché
```

**Code brut (ignoré par Knot):**
```typst
`r x`  →  Géré par Typst (monospace)
```

### Cache et inline

Les expressions inline participent au système de cache:
- Hash basé sur code + contexte chunk précédent
- Invalidation si variable change
- Pas de cache séparé (intégré au flux)

## Métriques

**Tests actuels:**
- Unit tests: 21
- Integration tests (basic): 5
- Integration tests (execution): 7 (requiert R)
- Integration tests (inline): 11 (2 parsing + 9 avec R)
- **Total:** 28 + 16 (avec R) = **44 tests** ✅

**Couverture à améliorer:**
- LSP (manque tests handlers)
- Backend (manque tests unitaires)
- Options validation (manque tests)

## Références

- REFERENCE.md (architecture complète)
- AI_CONTEXT.md (contexte pour LLM)
- README.md (documentation utilisateur)

---

**Dernière mise à jour:** 31 janvier 2026
**Statut projet:** Alpha (v0.1.0)
**Prochain milestone:** Phase 6 complète + tests LSP
