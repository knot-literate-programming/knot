# Knot Project Configuration

[document]
main = "main.knot"

[helpers]
# Typst helper functions (for rendering R/Python output)
typst = "lib/knot.typ"

# Note: R and Python helper scripts are now embedded in the binary
# and loaded automatically by the executors.

# Future: Use published Typst package instead of local copy
# typst = "@preview/knot"
