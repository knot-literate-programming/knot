Pour le cache :

* écriture atomique de metadata.json

* mécanisme pour supprimer les anciennes sorties de cache qui ne sont plus utilisées. Avec le temps, si le document est souvent modifié, le répertoire .knot_cache pourrait accumuler de nombreux fichiers orphelins et grossir considérablement.

* que faire avec les gros fichiers en cache 100 Mb * 50 chunks => gros pb de taille de cache.

* Le système suppose que les graphiques et dataframes sont déjà placés dans le répertoire du cache par l'exécuteur, ce qui crée un couplage fort.

* La logique pour récupérer des résultats composites (texte + graphique) est notée comme incomplète dans le code.

* Le champ document_hash dans les métadonnées ne semble pas être utilisé actuellement
