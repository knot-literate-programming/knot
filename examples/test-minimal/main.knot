// Import Knot helpers for R code execution
#import "lib/knot.typ": *

#import "@preview/codly-languages:0.1.1": *
#show: codly-init.with()
#codly(number-format: none)

// ============================================
// Document content
// ============================================

/* KNOT-INJECT-CHAPTERS */

```{r}
#|echo: false

library(ggplot2)
```

= My Document

Here is some text with an inline R expression: `{r} 1 + 1`.

```{r}
# Create some data
x <- 1:10
y <- x^3

# Display summary
summary(x)
```

#lorem(50).
The mean of `x` is `{r} mean(x, na.rm=FALSE)`.

```{r}
#| echo: true
# Create a simple plot
df <- data.frame(x = x, y = y)
gg <- ggplot(df, aes(x = x, y = y)) +
  geom_point(size = 3, color = "steelblue") +
  geom_line(color = "steelblue", alpha = 0.5) +
  theme_minimal() +
  labs(
    title = "Simple Quadratic Function",
    x = "x",
    y = "y = x²"
  )

# Render the plot (requires knot.r.package)
typst(gg, width = 4, height = 4)
```

```{python}
x = [1, 2, 3, 3]
print(x)
```

Voici la vie. Un peu de #emph[texte]. Toto. Tata. #lorem(200).

#lorem(20)
